# AI Resume Builder

Next.js app — see the root [README](../README.md) for the full walkthrough and setup guide.

## Quick Start

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

Requires a `.env.local` with `DEEPSEEK_API_KEY_1` set.
